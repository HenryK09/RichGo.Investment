set_index()[]
# 기존의 컬럼을 가지고 데이터프레임 인덱스 생성

.items()
# 인덱스와 값을 반환한다. for문에서 iterable한 상태를 유지하며 반환할 수 있는 마지막 값까지 원소를 필요할 때마다 하나씩 반환
# 자료구조를 반복문을 통해 원소 하나씩 인덱싱하는 것

f" <tmpV11>{code}</tmpV11>
# xml 데이터 내에서 중복되지 않는 값에 해당하는 부분을 f' {}로 처리

requests.post().text
# 웹에서 xml 데이터 request as text format

try & except
# 판매회사에 판매중인 펀드가 없어 데이터가 비어 있을 경우 에러 발생 -> 에러가 나면 빈 값을 가진 판매회사 코드와 회사 이름 반환

pd.read_xml
# xml 파일 읽기

loc[:, 'tmpV1':'tmpV18']
# 모든 인덱스를 불러오되, 원하는 범위의 컬럼만 가져오는

rename(columns={'':''})
# 컬럼명을 알아보기 쉽게 변경

to_csv({}.csv'.format(comp_name)
# csv 파일로 저장하는 과정에서 서로 다른 이름으로 파일 저장 하기 위해 사용